spring:
  application:
    name: ai-data-analyst
  profiles:
    active: dev

server:
  servlet:
    context-path: /api

logging:
  level:
    root: INFO
    com.aianalyst: DEBUG

security:
  jwt:
    issuer: ai-data-analyst
    expiration-seconds: 7200
